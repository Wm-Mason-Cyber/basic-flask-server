import pytest

pytest.skip("Selenium tests are optional and require additional setup (chromedriver).", allow_module_level=True)

# Placeholder: when enabled, tests would drive a headless browser to exercise XSS
